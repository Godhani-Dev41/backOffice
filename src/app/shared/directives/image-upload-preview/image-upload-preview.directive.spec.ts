import { ImageUploadPreviewDirective } from './image-upload-preview.directive';

describe('ImageUploadPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageUploadPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
